# Codex Insights v1.1.0 focused acceptance

## Verdict

**READY — v1.1.0 validated locally and by remote CI.**

The local release gates for the v1.1.0 correctness and safety hardening release passed. GitHub
Actions also passed on the exact release-preparation candidate. The annotated tag and public
GitHub Release have not yet been created or claimed here.

| Publication gate | State after candidate CI |
|---|---|
| Remote CI | **PASS** |
| Git tag `v1.1.0` | **NO** |
| Public GitHub Release | **NO** |
| Package registry | **NOT PUBLISHED** |

## Scope and repository state

Acceptance ran on 2026-08-12 from candidate starting HEAD
`be99d6c80218aaffc7d40bd430b8b7f0be944a55`. At preflight, `origin/main` and the peeled
`v1.0.0` tag both pointed to `163d54c359743bc98def1ac056f61d643f8d1e55`; the working tree was
clean and the candidate was three commits ahead and zero behind.

The release contains the forensic audit plus the two S-priority corrections it motivated:

- `ee418110e88a856ce03156bde0a8350006877692` — derived-index reset file-identity safety;
- `be99d6c80218aaffc7d40bd430b8b7f0be944a55` — conservative shell executable normalization.

The diff from `v1.0.0` contains the audit, the two affected production modules, their focused tests,
and directly supporting database/metrics/privacy documentation. No A-, B-, or C-priority forensic
implementation is included. Package version is `1.1.0`; derived schema remains 16 and the source
parser is `codex-source-parser-v9`.

## Reset identity-safety acceptance

**PASS — 11 focused synthetic tests.**

The reset/backup suite proved successful deletion when the validated index retained its identity and
fail-closed behavior for main-file replacement, symlink replacement, backup followed by replacement,
unavailable revalidation, new sidecars, and captured sidecar replacement. Replacement objects and
unrelated files survived rejected operations. Existing source-path/inode protections remained active,
and the disposable synthetic Codex source files were unchanged. The user's real derived index was
not reset.

## Executable-normalization acceptance

**PASS — 45 focused synthetic tool/command tests.**

Coverage includes simple and absolute commands, assignments, `env`, recognized wrappers,
`python -m pytest`, `uv run`, `cd &&`, `cd` alone, pipelines, `&&`, `||`, loops, conditionals,
grouping, heredocs, quoted paths containing spaces, scientific commands, process monitoring,
privacy redaction, repeated-command fingerprints, persistence, and parser-version reclassification.
Option/control heads including `-f`, `-v`, and `for` remain unresolved rather than being emitted as
executables.

## Read-only real aggregate smoke

The existing derived index was opened with SQLite read-only/query-only access. No command body was
printed and neither Codex source data nor the derived index was modified.

| Metric | Result |
|---|---:|
| Originated commands inspected | 34,244 |
| Known-invalid executable heads after normalization | 0 |
| Conservatively unresolved executable identities | 878 |
| Command fingerprints changed | 0 |
| Category total before/after | 34,244 / 34,244 |

The largest aggregate removals were `cd` (247 to 0), `for` (115 to 0), `-d` (59 to 0), `-v`
(58 to 0), and `-f` (18 to 0). `find` decreased from 931 to 764 because grouping-shaped syntax is
now treated conservatively. Originated-command accounting reconciled exactly; token accounting was
not changed.

## Local regression and package gates

| Gate | Result |
|---|---|
| Full pytest | **PASS — 244 passed, no failures or warnings** |
| Ruff | **PASS** |
| mypy | **PASS — 55 source files** |
| Wheel build/install | **PASS — `codex_insights-1.1.0-py3-none-any.whl`** |
| Source distribution build/install | **PASS — `codex_insights-1.1.0.tar.gz`** |
| Fresh import outside checkout | **PASS — imported from disposable site-packages** |
| Fresh CLI help/version/doctor | **PASS — reported 1.1.0** |
| Existing macOS editable guard | **PASS — no hidden-flag repair required** |

Wheel and source-distribution smoke tests used separate disposable Python 3.14.6 environments and
an empty explicit Codex home. Neither fresh environment imported from the repository checkout.

## Deferred v1.2 forensic backlog

The evidence-backed A/B/C findings remain deferred: structured source normalization, outcome
evidence calibration, incremental reconciliation cost, actionable unknown-source diagnostics,
conservative Git candidate refinement, SQL/orchestration efficiency, UNKNOWN taxonomy coverage,
deep-doctor presentation, accessibility, release documentation drift, Windows CI coverage,
maintainability hotspots, and consistent operational JSON. This release makes no claim to implement
them.

## Remote and publication state

GitHub Actions run
[`31595098956`](https://github.com/angzeli/codex-insights/actions/runs/31595098956) passed on exact
candidate SHA `e36e881a984939c419b36e3126826548d0ab1225`. Successful jobs covered Python 3.11,
3.12, 3.13, and 3.14 tests; Ruff and mypy; wheel/sdist build and fresh-install smoke; and macOS
normal/editable-install smoke. GitHub emitted only its upstream Node 20 action-deprecation notice;
no repository gate failed.

The evidence-only commit containing this update must itself receive successful remote CI before
`v1.1.0` is tagged or released. Its SHA and run are necessarily recorded by Git/GitHub rather than
self-referentially embedded in this file.
